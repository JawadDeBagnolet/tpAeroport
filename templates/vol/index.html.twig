<!DOCTYPE html>
<html lang="fr">
<head><title>Vols</title></head>
<body>

<h1>{% if message %}
        <p>{{ message }}</p>
    {% else %}
        <p>Aucun message disponible.</p>
    {% endif %}
</h1>
</body>
</html>
